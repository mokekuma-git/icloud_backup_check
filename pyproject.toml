[project]
name = "icloud-backup-check"
version = "0.1.0"
description = "Extract photos and videos from iTunes/iCloud backup with original quality preserved."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
